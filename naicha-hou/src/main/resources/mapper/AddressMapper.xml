<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naicha.hou.mapper.AddressMapper">

    <!-- 根据用户ID查询地址列表 -->
    <select id="selectByUserId" resultType="com.naicha.hou.entity.UserAddress">
        SELECT * FROM user_address 
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY is_default DESC, created_at DESC
    </select>

    <!-- 查询用户的默认地址 -->
    <select id="selectDefaultByUserId" resultType="com.naicha.hou.entity.UserAddress">
        SELECT * FROM user_address 
        WHERE user_id = #{userId} AND is_default = 1 AND deleted = 0
        LIMIT 1
    </select>

    <!-- 取消用户的所有默认地址 -->
    <update id="cancelDefaultByUserId">
        UPDATE user_address 
        SET is_default = 0, updated_at = NOW()
        WHERE user_id = #{userId} AND is_default = 1 AND deleted = 0
    </update>

</mapper>

